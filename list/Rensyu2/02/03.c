#include <stdio.h>

int main(void)
{
    int i;
    int ary[5];
    int tmp;
    int h,b;

    for (i=0; i < 5; i++){
        printf("ary[%d]: ", i);
        scanf("%d", &ary[i]);
    }
    for (h=0; h<4; h++){
      for(b=h+1; b<5; b++){
        if(ary[h] > ary)
      }
    }

    for(i=0; i < 5; i++){
        printf("ary[%d]=%d\n", i, ary[i]);
    }

    return 0;
}
